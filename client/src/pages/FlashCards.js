import react from 'react';
import axios from 'axios';
import LearningBoard from '../component/LearningCards/LearningBoard';

const FlashCards = () => {
  return <LearningBoard />;
};

export default FlashCards;
